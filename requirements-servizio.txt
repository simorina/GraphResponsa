# Dipendenze del solo servizio web.
#
# Fuori restano PyMuPDF, requests, beautifulsoup4 e lxml: servono a scaricare e
# parsare i PDF dal portale, cioe' alla pipeline, che gira in locale e non ha
# ragione di stare in un container di produzione. Toglierle alleggerisce
# l'immagine e riduce la superficie di attacco.

langchain>=1.0
langchain-neo4j
langchain-anthropic
# Il client dei modelli sul gateway compatibile OpenAI: DeepSeek, Qwen, Kimi,
# GLM. src/agente/agente.py lo importa solo quando serve, quindi finche' la
# produzione usava Claude la sua assenza non si vedeva: il 26/09, passati a
# DeepSeek, ogni domanda finiva in ModuleNotFoundError.
langchain-openai
langchain-voyageai
neo4j
# I contatori di consumo datano il giorno nel fuso di Roma, e python:3.12-slim
# non porta con se' il database dei fusi: senza questo, zoneinfo non trova
# "Europe/Rome" e il servizio non parte affatto.
tzdata
python-dotenv
fastapi
uvicorn[standard]

# Stato applicativo e identita' su AWS
boto3
langgraph-checkpoint-dynamodb
python-jose[cryptography]
